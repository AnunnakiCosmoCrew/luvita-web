---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/Hero.astro';
import Button from '../../components/Button.astro';
import { CONTACT_EMAIL } from '../../lib/site';
---

<BaseLayout
  locale="tr"
  title="İletişim — Luvita"
  description="Luvita ile iletişime geçin — bize e-posta gönderin, bir gün içinde dönüş yapalım."
  paths={{ tr: 'iletisim', en: 'contact' }}
>
  <Hero
    eyebrow="İletişim"
    title="Bize ulaşın"
    lead="Kısa bir e-posta yeterli — aklınızdakini birkaç cümleyle yazın, bir gün içinde dönüş yapalım."
  >
    <Fragment slot="actions">
      <Button href={`mailto:${CONTACT_EMAIL}`}>E-posta gönderin</Button>
    </Fragment>
  </Hero>

  <section class="container">
    <div class="contact-grid">
      <div class="contact-card">
        <h2>E-posta</h2>
        <p><a href={`mailto:${CONTACT_EMAIL}`}>{CONTACT_EMAIL}</a></p>
        <p class="contact-note">Bodrum’dan uzaktan çalışıyoruz.</p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .contact-grid {
    display: grid;
    gap: var(--sp-5);
    max-width: 24rem;
  }

  .contact-card {
    padding: var(--sp-5);
    background-color: var(--c-surface);
    border: 1px solid var(--c-border);
    border-radius: var(--radius);
  }

  .contact-card h2 {
    font-size: var(--fs-500);
    margin-bottom: var(--sp-2);
  }

  .contact-card p {
    color: var(--c-ink-soft);
  }

  .contact-note {
    margin-top: var(--sp-2);
    font-size: var(--fs-300);
    color: var(--c-muted);
  }
</style>
