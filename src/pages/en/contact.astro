---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/Hero.astro';
import Button from '../../components/Button.astro';
import { ADDRESS, CONTACT_EMAIL } from '../../lib/site';
---

<BaseLayout
  locale="en"
  title="Contact — Luvita"
  description="Get in touch with Luvita: send us an email to talk about your project."
  paths={{ tr: 'iletisim', en: 'contact' }}
>
  <Hero
    eyebrow="Contact"
    title="Let's talk about your project"
    lead="A short email is enough — describe what you need in a few sentences and we'll get back to you within a day."
  >
    <Fragment slot="actions">
      <Button href={`mailto:${CONTACT_EMAIL}`}>Send an email</Button>
    </Fragment>
  </Hero>

  <section class="container">
    <div class="contact-grid">
      <div class="contact-card">
        <h2>Email</h2>
        <p><a href={`mailto:${CONTACT_EMAIL}`}>{CONTACT_EMAIL}</a></p>
      </div>
      <div class="contact-card">
        <h2>Address</h2>
        <p>{ADDRESS}</p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .contact-grid {
    display: grid;
    gap: var(--sp-5);
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 18rem), 1fr));
  }

  .contact-card {
    padding: var(--sp-5);
    background-color: var(--c-surface);
    border: 1px solid var(--c-border);
    border-radius: var(--radius);
  }

  .contact-card h2 {
    font-size: var(--fs-500);
    margin-bottom: var(--sp-2);
  }

  .contact-card p {
    color: var(--c-ink-soft);
  }
</style>
